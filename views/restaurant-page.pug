html(lang='en')
  head 
    title #{restaurant.name}
    script(src="https://cdnjs.cloudflare.com/ajax/libs/axios/0.19.0/axios.min.js")
    script(type="text/javascript" src="/js/restaurant_page.js")
  body(id='maincontent' onload=`init(${restaurant.id})`)
    include ./partials/header
    #restaurant_info
      include ./partials/restaurant_info
    - var menu_length = Object.keys(restaurant.menu).length
    #menu.row 
      include ./partials/menu_constructor.pug
      #update_content(style="text-align:right;").column
        include ./partials/update_restaurant.pug
    